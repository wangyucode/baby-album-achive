<!-- pages/album/album.wxml -->
<view class='container-album'>
  <image wx:if="{{albums==null||albums.length==0}}" class="no-album-image" src=""></image>
  <block wx:else>
    <view class="album" wx:for="{{albums}}"></view>
  </block>
</view>